This fcgitest is just the example fastcgi SDK echo.c example copied here (licenses are compatible).

Purpose is to get compiling and linking all working and serve as an example.

After we get odata fastcgi plugin up and running, we can remove this directory.
